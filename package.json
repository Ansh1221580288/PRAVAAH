{
  "name": "pravaah-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/authority && npm install && npm run build",
    "dev": "cd frontend/authority && npm run dev"
  }
}
